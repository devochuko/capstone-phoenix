---
# infra/ansible/requirements.yml
#
# Install with: ansible-galaxy collection install -r requirements.yml
#
# community.general.ufw      — used by roles/hardening
# ansible.posix.authorized_key — used by roles/hardening

collections:
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
